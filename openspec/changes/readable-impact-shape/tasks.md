## 1. Skill Shape

- [x] 1.1 Rewrite the paragraph above `## Steps` in `templates/osi-impact/SKILL.md`: user language, chat only, four-section user-path deliverable; drop per-finding `cause → 表现`
- [x] 1.2 Replace `### Shape` with the Visualize box (before the skeleton), four sections (故意没动 in 收尾), and five clustered bans from design.md. Keep: no YAML dump, no path table as main output
- [x] 1.3 Replace examples with PermButton + PermCheck: planned uses「若改」; partial/done has tiny ASCII (状态机 + 入口表 with one 不动 cell), one 表现, one 漏改, one 故意没动, one 上线注意. One forbidden opener. No 维修保洁 nouns
- [x] 1.4 Diff Steps 1–7 and the「Need another file」line against the previous template: unchanged. Frontmatter still has `disable-model-invocation: true` and slash-only firing

## 2. Read-through

- [x] 2.1 Read only Shape as someone who has not seen the repo: it must produce 一句话 + two ASCII + short 表现, not a file list. If Shape is longer than Steps, cut bans before cutting example diagrams
