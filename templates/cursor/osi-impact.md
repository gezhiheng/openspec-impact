---
name: '/osi-impact'
id: 'osi-impact'
---

Follow the `osi-impact` skill. Usage: `/osi-impact {change}`

`{change}` is a live OpenSpec change id or path (`add-renewal-status`, `openspec/changes/add-renewal-status`). Run `osi impact {change}` for YAML evidence.
